package io.github.opsord.architecture_evaluator_backend;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class ArchitectureEvaluatorBackendApplicationTests {

	/**
	 * Test that verifies if the Spring application context loads successfully.
	 * This ensures that the application starts without configuration errors.
	 */
	@Test
	void contextLoads() {
		// This test will pass if the application context loads successfully.
		// No additional assertions are needed as this is a context load test.
		// If there are issues with the configuration, the test will fail.
		// The actual logic of the application is not tested here, only the context loading.
	}

}
